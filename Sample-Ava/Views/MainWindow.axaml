<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Sample_Ava.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:l="urn:pattech.words"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="Sample_Ava.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Title="Sample_Ava">

    <Design.DataContext>
        <!-- This only sets the DataContext for the previewer in an IDE,
             to set the actual DataContext for runtime, set the DataContext property in code (look at App.axaml.cs) -->
        <vm:MainWindowViewModel/>
    </Design.DataContext>
    
  <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
    <TextBlock>
      <l:WordsInline Key="main.Markdown"/>
    </TextBlock>

    <TextBlock>
      asdf<Span>
        <Run BaselineAlignment="Subscript" FontSize="10">dn</Run></Span><Span TextDecorations="Underline">asdf</Span>
    </TextBlock>
  </StackPanel>
</Window>
