<UserControl x:Class="WordsEdit.Views.GripesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WordsEdit.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:GripesViewModel}"
             Padding="10px">
    <StackPanel MinWidth="420" MaxWidth="760">
        <TextBlock Text="{Binding Title, Mode=OneWay}" FontWeight="Bold" FontSize="16" Margin="2.5px"/>
        <TextBox Text="{Binding Text, Mode=OneWay}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 MaxHeight="400"
                 FontFamily="Consolas"
                 Margin="2.5px"/>
        <Button Content="Close" Command="{Binding CloseCommand}" HorizontalAlignment="Right" Width="100" Margin="2.5px 10px 2.5px 2.5px"/>
    </StackPanel>
</UserControl>
